# Setup Notes for Gandhi Fellowship Job Portal

## ✅ Completed Changes

### 1. SQL Migration Files Created
- `supabase/migrations/20250101000000_create_jobs_and_applications.sql` - Main schema with clean table names (`jobs`, `applications`)
- `supabase/migrations/20250101000001_create_admin_column_definitions.sql` - Admin column definitions table

**Table Names:**
- `jobs`
- `applications`
- `admin_column_definitions`

### 2. Code Updates
- All code references updated to use new table names
- Brand colors updated in `tailwind.config.ts`:
  - Primary (Grey): `#344354`
  - Secondary (Green): `#71B941`
  - Accent (Orange): `#F24F00`
- Organization name updated to "Gandhi Fellowship" throughout
- Tagline added: "unfold impact enjoy"
- Logo references updated to `/gandhi-fellowship-logo.png`

### 3. Branding Updates
- Email templates updated with Gandhi Fellowship branding
- UI components updated with new organization name
- Website URL: `https://gandhifellowship.org`
- Contact email: `careers@gandhifellowship.org`

## 📋 Next Steps

### 1. Add Logo File
Place the Gandhi Fellowship logo file in the `public/` directory as:
- `public/gandhi-fellowship-logo.png`

The logo should be the one provided (Gandhi Fellowship logo with the green ribbon shape).

### 2. Environment Variables Setup
Set up the following environment variables (as mentioned, you'll do this later):
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `RESEND_API_KEY`
- `ADMIN_EMAIL`
- `ADMIN_EMAILS`
- `FROM_EMAIL`

### 3. Database Setup
Run the SQL migration files in your Supabase project:
1. `20250101000000_create_jobs_and_applications.sql`
2. `20250101000001_create_admin_column_definitions.sql`

### 4. Update TypeScript Types (Optional)
The `src/integrations/supabase/types.ts` file may need to be regenerated after setting up the database. This is typically auto-generated by Supabase CLI:
```bash
npx supabase gen types typescript --project-id YOUR_PROJECT_ID > src/integrations/supabase/types.ts
```

## 🎨 Brand Colors Reference

- **Orange**: `#F24F00` (RGB: 242, 79, 0)
- **Grey**: `#344354` (RGB: 52, 67, 84)
- **Green**: `#71B941` (RGB: 113, 185, 65)

These colors are now configured in `tailwind.config.ts` and used throughout the application.
